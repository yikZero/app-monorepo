// Placeholder used when PRIME_DEMO_ENABLED is unset so Metro can satisfy the
// gated require() in apps/mobile/index.ts without pulling the demo scene.
export default function PrimeDemoDisabled() {
  return null;
}
